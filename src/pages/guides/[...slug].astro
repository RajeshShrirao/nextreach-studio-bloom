---
import Layout from "@/layouts/Layout.astro";
import Nav from "@/components/Nav.astro";
import Footer from "@/components/Footer.astro";
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  const guides = await getCollection("guides", ({ data }) => !data.draft);
  return guides.map((guide) => ({
    params: { slug: guide.slug },
    props: { guide },
  }));
}

const { guide } = Astro.props;
const { Content } = await guide.render();
---

<Layout
  title={guide.data.title}
  description={guide.data.description}
  image={guide.data.image}
  type="article"
  pubDate={guide.data.pubDate}
  author={guide.data.author}
  tags={guide.data.tags}
  canonicalURL={guide.data.canonicalURL}
>
  <Nav />
  <main class="pt-28 pb-20">
    <article class="px-6 sm:px-8 max-w-3xl mx-auto">
      <header class="mb-12">
        <div class="flex flex-wrap items-center gap-3 mb-6">
          <a href="/guides" class="text-xs text-zinc-600 hover:text-zinc-400 transition-colors">
            ← All guides
          </a>
          <span class="text-zinc-800">·</span>
          <span class={`rounded-full px-2 py-0.5 text-xs font-medium capitalize ${
            guide.data.difficulty === "beginner"
              ? "bg-emerald-500/10 text-emerald-400 border border-emerald-700/30"
              : guide.data.difficulty === "advanced"
              ? "bg-red-500/10 text-red-400 border border-red-700/30"
              : "bg-blue-500/10 text-blue-400 border border-blue-700/30"
          }`}>
            {guide.data.difficulty}
          </span>
          {guide.data.readingTime && (
            <span class="text-xs text-zinc-600">{guide.data.readingTime}</span>
          )}
        </div>

        <h1 class="font-display font-semibold text-3xl sm:text-4xl lg:text-5xl text-white tracking-[-0.01em] leading-[1.2] mb-4">
          {guide.data.title}
        </h1>
        <p class="text-lg text-zinc-400 leading-relaxed mb-6">{guide.data.description}</p>

        <div class="flex flex-wrap items-center gap-3 text-xs text-zinc-600">
          <time datetime={guide.data.pubDate.toISOString()}>
            {guide.data.pubDate.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}
          </time>
          {guide.data.updatedDate && (
            <>
              <span>·</span>
              <span>Updated {guide.data.updatedDate.toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" })}</span>
            </>
          )}
          <span>·</span>
          <span>{guide.data.author}</span>
          {guide.data.tags.length > 0 && (
            <>
              <span>·</span>
              <div class="flex gap-1.5">
                {guide.data.tags.map((tag) => (
                  <span class="rounded-full bg-zinc-800 px-2 py-0.5 text-zinc-500">{tag}</span>
                ))}
              </div>
            </>
          )}
        </div>
      </header>

      <div class="glass-panel rounded-2xl p-8 sm:p-10 mb-12">
        <div class="prose prose-invert max-w-none
          prose-headings:text-white prose-headings:font-display prose-headings:font-semibold
          prose-h2:text-2xl prose-h2:mt-10 prose-h2:mb-4
          prose-h3:text-lg prose-h3:mt-6 prose-h3:mb-3
          prose-p:text-zinc-400 prose-p:leading-relaxed
          prose-a:text-amber-400 prose-a:no-underline hover:prose-a:text-amber-300
          prose-strong:text-white
          prose-code:text-amber-400 prose-code:bg-zinc-800 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:text-sm
          prose-pre:bg-zinc-900 prose-pre:border prose-pre:border-zinc-800 prose-pre:rounded-xl
          prose-blockquote:border-amber-400 prose-blockquote:text-zinc-400
          prose-ul:text-zinc-400 prose-ol:text-zinc-400
          prose-li:marker:text-zinc-600
          prose-table:text-sm prose-th:text-zinc-300 prose-td:text-zinc-400 prose-th:bg-zinc-900 prose-tr:border-zinc-800
        ">
          <Content />
        </div>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 justify-between items-start border-t border-zinc-800 pt-8">
        <a href="/guides" class="inline-flex items-center gap-2 text-amber-400 hover:text-amber-300 transition-colors text-sm font-medium">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-4 h-4"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
          Back to guides
        </a>
        <a href="/tools" class="inline-flex items-center gap-2 text-zinc-500 hover:text-zinc-300 transition-colors text-sm">
          Browse free tools →
        </a>
      </div>
    </article>
  </main>
  <Footer />
</Layout>
